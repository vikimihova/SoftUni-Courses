function solve(text, startIndex, indexCount) {
    let textSubstring = text.substring(startIndex, startIndex + indexCount);

    console.log(textSubstring);
}

solve('ASentence', 1, 8);
solve('SkipWord', 4, 7);